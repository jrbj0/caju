import { FilterTranscriptions } from './filter-transcriptions';

describe('FilterTranscriptions', () => {
  it('should create an instance', () => {
    expect(new FilterTranscriptions()).toBeTruthy();
  });
});
